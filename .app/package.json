{
  "name": "app",
  "private": true,
  "type": "module",
  "devDependencies": {
    "nuxt": "3.9.1"
  },
  "scripts": {
    "prepare": "nuxi prepare",
    "dev": "nuxi dev --open",
    "build": "nuxi build",
    "generate": "nuxi generate",
    "clean": "rimraf .nuxt .output node_modules"
  }
}
